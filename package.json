{
  "private": true,
  "scripts": {
    "deploy": "node scripts/reset.js && next build && next export && node scripts/publish.js",
    "deploy2": "node scripts/reset.js && rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy Next.js to gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "gh-pages": "^2.0.1",
    "next": "^7.0.2",
    "next-offline": "^3.2.2",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "rmdir": "^1.2.0"
  }
}
