{
  "private": true,
  "scripts": {
    "deploy": "node scripts/reset.js && next build && next export && node scripts/publish.js"
  },
  "dependencies": {
    "gh-pages": "^2.0.1",
    "next": "^7.0.2",
    "next-offline": "^3.2.2",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "rmdir": "^1.2.0"
  }
}
